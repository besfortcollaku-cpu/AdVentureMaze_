.chest-box {
  text-align:center;
}

.chest-icon {
  font-size:70px;
  margin:20px 0;
}

.chest-open {
  font-size:60px;
  margin:20px 0;
}

.spin {
  animation:spin 1s linear infinite;
}

.reward-coins {
  font-size:24px;
  font-weight:bold;
  color:#ffd700;
}

@keyframes spin {
  from { transform:rotate(0deg); }
  to { transform:rotate(360deg); }
}